/**
 * tRPC client-side exports for the RandoDrum application
 * For server-side usage, import directly from './server'
 */

export { api, TRPCReactProvider } from './react';
export { createQueryClient } from './query-client';
